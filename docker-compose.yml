version: "3.5"

services:
    backend:
      build: ./backend
      ports : 
        - "5000:5000" 
      hostname :  backend-host

    frontend:
        build: ./frontend
        ports: 
          - "3000:3000"
        depends_on: 
          - backend
        hostname:  frontend-host
